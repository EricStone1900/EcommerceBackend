apiVersion: v1
kind: Service
metadata:
  name: ecommerce-app
  namespace: default
  labels:
    app: ecommerce-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ecommerce-app
